Modifier le fichier: node_modules/react-native-reanimated/android/build.gradle

1. Trouvez ce bloc de code (vers la ligne 387):

```gradle
def assertReactNativeVersion = tasks.register("assertMinimalReactNativeVersionTask") {
  doLast {
    if (reactNativeMajor < 75) {
      throw new GradleException("[Reanimated] Unsupported React Native version. Please use 75. or newer.")
    }
  }
}
```

2. Remplacez-le par:

```gradle
def assertReactNativeVersion = tasks.register("assertMinimalReactNativeVersionTask") {
  doLast {
    if (reactNativeMajor < 70) {
      throw new GradleException("[Reanimated] Unsupported React Native version. Please use 70. or newer.")
    }
  }
}
```

Cette modification réduit la version minimale requise de React Native de 75 à 70, ce qui devrait être compatible avec la version que vous utilisez.
